import React from 'react';

import { Collapse } from '../Collapse';

const Variants = () => {
  return (
    <div style={{ width: '100%' }}>
      <Collapse expandButton fullscreenButton leftSide="Заголовок таблицы">
        sdfsdf
      </Collapse>
    </div>
  );
};

export default Variants;
